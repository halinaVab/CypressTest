version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          name: Production tests
          record: true
          spec: cypress/integration/amazonTests/*
          filters:
              branches:
                only:
                  - circleciintegration